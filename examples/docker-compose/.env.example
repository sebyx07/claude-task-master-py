# =============================================================================
# Claude Task Master - Environment Variables Template
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# IMPORTANT: Never commit .env to version control!
#   echo ".env" >> .gitignore
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED FOR PRODUCTION
# -----------------------------------------------------------------------------

# Password for REST API and MCP server authentication
# Generate a secure password: openssl rand -base64 32
CLAUDETM_PASSWORD=your-secure-password-min-16-chars

# -----------------------------------------------------------------------------
# PATHS
# -----------------------------------------------------------------------------

# Path to your project directory (where Claude Task Master will work)
# Default: current directory
PROJECT_PATH=/path/to/your/project

# Path to Claude credentials directory (contains OAuth tokens)
# Default: ~/.claude
CLAUDE_CREDENTIALS_PATH=~/.claude

# -----------------------------------------------------------------------------
# SERVER CONFIGURATION
# -----------------------------------------------------------------------------

# Log level: debug, info, warning, error
# Default: info
CLAUDETM_LOG_LEVEL=info

# REST API port
# Default: 8000
CLAUDETM_REST_PORT=8000

# MCP server port
# Default: 8080
CLAUDETM_MCP_PORT=8080

# MCP transport: sse or streamable-http
# Default: sse
CLAUDETM_MCP_TRANSPORT=sse

# Server bind host (should be 0.0.0.0 in containers)
# Default: 0.0.0.0
CLAUDETM_SERVER_HOST=0.0.0.0

# -----------------------------------------------------------------------------
# WEBHOOKS (OPTIONAL)
# -----------------------------------------------------------------------------

# Webhook URL for notifications
# Leave empty to disable webhooks
CLAUDETM_WEBHOOK_URL=https://webhook.example.com/claudetm

# Webhook secret for HMAC signature verification
# Generate: openssl rand -hex 32
CLAUDETM_WEBHOOK_SECRET=your-webhook-secret-key

# -----------------------------------------------------------------------------
# CORS (OPTIONAL)
# -----------------------------------------------------------------------------

# Comma-separated list of allowed CORS origins
# Leave empty to disable CORS
CLAUDETM_CORS_ORIGINS=http://localhost:3000,https://app.example.com

# -----------------------------------------------------------------------------
# TASK CONFIGURATION (OPTIONAL)
# -----------------------------------------------------------------------------

# Default target branch for pull requests
# Default: main
CLAUDETM_TARGET_BRANCH=main

# Auto-merge PRs when CI passes and approved
# Default: true
CLAUDETM_AUTO_MERGE=true

# Maximum number of task sessions
# Leave empty for unlimited
CLAUDETM_MAX_SESSIONS=10

# -----------------------------------------------------------------------------
# REVERSE PROXY (FOR NGINX/CADDY EXAMPLES)
# -----------------------------------------------------------------------------

# Your domain name
DOMAIN=claudetm.example.com

# SSL certificate paths (for Nginx)
SSL_CERT_PATH=/etc/letsencrypt/live/claudetm.example.com/fullchain.pem
SSL_KEY_PATH=/etc/letsencrypt/live/claudetm.example.com/privkey.pem

# -----------------------------------------------------------------------------
# MONITORING (FOR MONITORING EXAMPLE)
# -----------------------------------------------------------------------------

# Grafana admin credentials
# Default: admin/admin
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=your-secure-grafana-password

# -----------------------------------------------------------------------------
# EXAMPLES
# -----------------------------------------------------------------------------

# Minimal production setup:
#   CLAUDETM_PASSWORD=secure-password
#   PROJECT_PATH=/home/user/my-project
#
# Full production setup:
#   CLAUDETM_PASSWORD=secure-password
#   PROJECT_PATH=/home/user/my-project
#   CLAUDETM_WEBHOOK_URL=https://webhook.example.com
#   CLAUDETM_WEBHOOK_SECRET=webhook-secret
#   CLAUDETM_LOG_LEVEL=info
#   CLAUDETM_MAX_SESSIONS=20
#
# Development setup:
#   PROJECT_PATH=../../tmp/test-project
#   CLAUDETM_LOG_LEVEL=debug
#   # No password in development (insecure!)
#
# With reverse proxy:
#   CLAUDETM_PASSWORD=secure-password
#   DOMAIN=claudetm.example.com
#   SSL_CERT_PATH=/etc/letsencrypt/live/claudetm.example.com/fullchain.pem
#   SSL_KEY_PATH=/etc/letsencrypt/live/claudetm.example.com/privkey.pem
#
# =============================================================================
